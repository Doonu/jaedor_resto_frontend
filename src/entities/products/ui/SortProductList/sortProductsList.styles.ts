import { Select } from 'antd';
import styled from 'styled-components';

export const SSortProductsList = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
`;

export const STitle = styled.div`
  font-size: 20px;
  font-weight: bold;
`;

export const SSelect = styled(Select)`
  min-width: 200px;
`;
