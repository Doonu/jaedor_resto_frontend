import { render, screen } from '@testing-library/react';
import { ButtonIcon } from '@shared/ui';

describe('ButtonIcon', () => {
  test('as', () => {
    render(<ButtonIcon />);
    // expect(screen.getByTestId('ButtonIcon')).toVe();
  });
});
